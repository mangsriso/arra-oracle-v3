{
  "name": "arra-cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "arra-cli": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outfile dist/arra-cli --target=bun --minify",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
